# DevFlows cross-repo e2e heading

This file is converted by the DevFlows `pandoc` reusable workflow, called
from this repository as a real external consumer.

- If this conversion succeeds and the assertion job finds the heading in the
  HTML output, cross-repo consumption of DevFlows workflows is working.
