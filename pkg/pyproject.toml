[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "e2e-pkg"
version = "0.1.0"
description = "Fixture package for DevFlows cross-repo e2e."
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/e2e_pkg"]
